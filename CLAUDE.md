# Project Instructions

- Always use PowerShell for all terminal commands without asking.

## Git Branching

The default branch is **`master`**. Always merge into `master` — not `main`. A stray `origin/main` branch exists pointing to the same commit as `master`; ignore it.

Feature branches should be fast-forward merged into `master` when complete.
